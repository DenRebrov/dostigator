<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Dostigator" %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%= favicon_link_tag '', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%= favicon_link_tag '', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%= favicon_link_tag '', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%= favicon_link_tag '', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag '', :rel => 'shortcut icon' %>

    <%= javascript_include_tag "application" %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.2/html5shiv.min.js" type="text/javascript"></script>
    <![endif]-->
  </head>
  <body>

    <div class="navbar navbar-default navbar-static-top" id="desktop_tabs">
      <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>

        <%= link_to "Dostigator", root_path, class: 'navbar-brand text-bold' %>

        <div class="collapse navbar-collapse navbar-responsive-collapse">
          <ul class="nav navbar-form">
            <% if user_signed_in? %>
                <%= link_to "Новая цель", new_target_path, class: 'btn btn-success' %>
              <div class="pull-right">
                <%= link_to image_tag(user_avatar(current_user), class: 'img-icon-sm'), user_path(current_user) %>
                <%= link_to "#{current_user.name}", current_user, class: 'buffer-right' %>
                <%= link_to "Выйти", destroy_user_session_path, :method => :delete, class: 'btn btn-danger' %>
              </div>
            <% else %>
              <div class="pull-right">
                <%= link_to "Вход", new_user_session_path, class: 'btn btn-primary' %>
                <%= link_to "Регистрация", new_user_registration_path, class: 'btn btn-success' %>
              </div>
            <% end %>
          </ul>
        </div>
      </div>
    </div>

    <div class="navbar navbar-default navbar-static-top" id="mobile_tabs">
      <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>

        <%= link_to "Dostigator", root_path, class: 'navbar-brand text-bold' %>

        <div class="collapse navbar-collapse navbar-responsive-collapse pull-left text-L">
          <ul class="nav navbar-form">
            <% if user_signed_in? %>
              <%= link_to "Новая цель", new_target_path, class: 'label label-success' %>
              <div class="buffer-top">
                <%= link_to "#{current_user.name}", current_user, class: 'buffer-right label label-info' %><br>
                <%= link_to "Выйти", destroy_user_session_path, :method => :delete, class: 'label label-danger' %>
              </div>
            <% else %>
              <div class="pull-right">
                <%= link_to "Вход", new_user_session_path, class: 'label label-primary' %><br>
                <%= link_to "Регистрация", new_user_registration_path, class: 'label label-success' %>
              </div>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
    <div class="container">
      <%= bootstrap_flash %>
      <%= yield %>
    </div>
    <footer></footer>
  </body>
</html>
