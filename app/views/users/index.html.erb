<div class="text-center text-primary">
  <div class="text-title" id="desktop_tabs">
    <p>Достигаторы</p>
  </div>
  <div class="text-subtitle" id="mobile_tabs">
    <p>Достигаторы</p>
  </div>
  <%= link_to "Показать все цели", targets_path, class: 'btn btn-default buffer-bottom' %>
</div>

<div class="text-center text-word-breaker" id="desktop_tabs">
  <% if @users.present? %>
    <% @users.order(:created_at).reverse_order.each do |user| %>
      <div class="users-list-item">
        <div class="text-bold text-M">
          <%= link_to user.name, user_path(user), class: 'img-icon text-dark-green buffer-right-max' %>
        </div>
        <%= link_to image_tag(user_avatar(user), class: 'img-icon buffer-right buffer-right-max'), user_path(user) %>
      </div>
    <% end %>
  <% else %>
    <div class="text-L text-black">
      <p>Пока нет :(</p>
    </div>
  <% end %>
</div>

<div class="text-word-breaker" id="mobile_tabs">
  <% if @users.present? %>
    <% @users.order(:created_at).reverse_order.each do |user| %>
      <div class="user-border buffer-bottom">
        <%= link_to image_tag(user_avatar(user), class: 'img-icon-sm'), user_path(user) %>
        <%= link_to user.name, user_path(user), class: "btn text-bold" %><br>
        <%= user.created_at %>
      </div>
    <% end %>
  <% else %>
    <div class="text-L text-black">
      <p>Пока нет :(</p>
    </div>
  <% end %>
</div>

      <!--<div class="container">-->
      <!--<div class="row">-->
      <!--<div class="table-target-border buffer-bottom col-md-6">-->
      <!--<%#= link_to user.name, user_path(user), class: 'img-icon-sm text-dark-green buffer-right' %>-->
      <!--</div>-->
      <!--<div class="col-md-6">-->
      <!--<%#= link_to image_tag(user_avatar(user), class: 'img-icon-sm'), user_path(user) %>-->
      <!--</div>-->
      <!--</div>-->
      <!--</div>-->

